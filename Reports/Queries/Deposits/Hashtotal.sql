SELECT
(
  SELECT
    COALESCE(SUM(ACCT_WAR_AVG_MTD_LGR_BAL), 0) AS ACCT_WAR_AVG_MTD_LGR_BAL,
    COALESCE(SUM(BASE_EQUIVALENT), 0) AS BASE_EQUIVALENT,
    COALESCE(SUM(PRINCIPAL_BASE_EQ), 0) AS PRINCIPAL_BASE_EQ,
    COALESCE(SUM(DISCOUNT_GIVEN_BASE_EQ), 0) AS DISCOUNT_GIVEN_BASE_EQ,
    COALESCE(SUM(TRANS_ACCT_BASE_EQ), 0) AS TRANS_ACCT_BASE_EQ
  FROM
    RB_V_HST_DEPOSITS S
  WHERE
    (
      USERUPD = ${user.code}
      AND (EXISTS(SELECT 1 FROM RB_V_HST_DEPOSITS WHERE USERUPD = ${user.code}))
    )
    OR (
      DATEUPD = (SELECT MAX(DATEUPD) FROM RB_V_HST_DEPOSITS)
      AND (NOT EXISTS(SELECT 1 FROM RB_V_HST_DEPOSITS WHERE USERUPD = ${user.code}))
    )
) AS STG,
(
  SELECT
    COALESCE(SUM(IMPORTO_5), 0) AS ACCT_WAR_AVG_MTD_LGR_BAL,
    COALESCE(SUM(IMPORTO_2), 0) AS BASE_EQUIVALENT,
    COALESCE(SUM(IMPORTO_11), 0) AS PRINCIPAL_BASE_EQ,
    COALESCE(SUM(IMPORTO_12), 0) AS DISCOUNT_GIVEN_BASE_EQ,
    COALESCE(SUM(IMPORTO_13), 0) AS TRANS_ACCT_BASE_EQ
  FROM
    FORM_DATI A
    INNER JOIN
    (
      SELECT DISTINCT
        RB_F_TGK_CREATE_SCENARIO_CODE(MONTH_END_DATE) AS COD_SCENARIO,
        RB_F_TGK_CREATE_PERIOD_CODE(MONTH_END_DATE) AS COD_PERIODO,
        REPORTING_ENTITY
      FROM
        RB_V_HST_DEPOSITS S
      WHERE
        (
          USERUPD = ${user.code}
          AND (EXISTS(SELECT 1 FROM RB_V_HST_DEPOSITS WHERE USERUPD = ${user.code}))
        )
        OR (
          DATEUPD = (SELECT MAX(DATEUPD) FROM RB_V_HST_DEPOSITS)
          AND (NOT EXISTS(SELECT 1 FROM RB_V_HST_DEPOSITS WHERE USERUPD = ${user.code}))
        )
    ) S
  ON
	A.COD_SCENARIO = S.COD_SCENARIO
	AND A.COD_PERIODO = S.COD_PERIODO
	AND A.TESTO_19 = S.REPORTING_ENTITY
  WHERE
    A.COD_PROSPETTO = 'DEPOSITS'
) AS APP
FROM
  DUAL
